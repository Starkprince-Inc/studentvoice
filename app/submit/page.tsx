import { SiteHeader } from "../components/SiteHeader";

export const metadata = { title: "Submit evidence", description: "Submit a public link or prepare an original file for protected evidence intake." };

export default function SubmitPage() {
  return <><SiteHeader /><main><header className="page-hero shell"><p className="eyebrow">Protected intake · सुरक्षित जमा</p><h1>Share what you documented.</h1><p className="lede">This public demo prepares a submission manifest. Production uploads are completed directly to quarantined, access-controlled storage using short-lived resumable URLs.</p></header><div className="shell form-shell"><div className="notice"><strong>Before you submit:</strong> Keep the original file unchanged. Do not rename, trim, transcode, screenshot, or remove metadata. Do not upload media you do not have the right to provide.</div><form className="form-card" action="/api/submission-demo" method="post"><div className="field-grid"><div className="field"><label htmlFor="alias">Protected alias</label><input id="alias" name="alias" placeholder="Optional pseudonym" autoComplete="off" /><span className="hint">Leave blank for a one-time anonymous receipt.</span></div><div className="field"><label htmlFor="kind">Evidence type</label><select id="kind" name="kind" defaultValue="link"><option value="link">Public link</option><option value="original">Original file manifest</option></select></div></div><div className="field"><label htmlFor="source-url">Public source URL</label><input id="source-url" name="source_url" type="url" placeholder="https://…" /></div><div className="field"><label htmlFor="context">What does this show?</label><textarea id="context" name="context" placeholder="Describe when, where, and how the recording was made. Separate what you saw from what someone told you." /></div><div className="field"><label htmlFor="contact">Safe follow-up channel</label><input id="contact" name="contact" placeholder="Optional email or secure handle" autoComplete="off" /><span className="hint">In production this field is encrypted separately and never included in a public record.</span></div><label className="check-row"><input type="checkbox" name="rights_confirmed" required /><span>I created this evidence or have permission to provide it for preservation and review.</span></label><label className="check-row"><input type="checkbox" name="safety_confirmed" required /><span>I understand that approved public evidence will use a reviewed, redacted derivative rather than the protected original.</span></label><button className="button button-primary" type="submit">Create protected receipt</button></form></div></main></>;
}
